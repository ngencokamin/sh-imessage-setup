mkmf.log
# The bbctl binary, which setup.sh drops at the repo root. Anchored: unanchored
# `bbctl*` matches at every depth, so it would silently ignore any bbctl-prefixed
# file added in a subdirectory later.
/bbctl*

# Generated by setup-tailscale-serve.sh — contains secrets, never commit
config.env
run-bridge.sh
